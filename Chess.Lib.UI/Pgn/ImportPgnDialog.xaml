<dlg:DialogView x:Class="Chess.Lib.UI.Pgn.ImportPgnDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chess.Lib.UI.Pgn"
             xmlns:dlg="clr-namespace:Common.Lib.UI.Dialogs;assembly=Common.Lib.UI"
						 xmlns:ctrls="clr-namespace:Common.Lib.UI.Controls;assembly=Common.Lib.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Header="Import PGN" MaxWidth="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" MaxHeight="300"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<local:PgnTagEditor Grid.Row="0" DataContext="{Binding Tags}"/>
		<GroupBox Grid.Row="1" Header="Moves">
			<TextBox Style="{StaticResource multiLine}" Text="{Binding Moves}"/>
		</GroupBox>
		<DockPanel Grid.Row="2" LastChildFill="False">
			<Button Content="Cancel" Command="{Binding Command}" CommandParameter="cancel" IsCancel="True"/>
			<StackPanel VerticalAlignment="Center" Margin="5,2">
				<ctrls:Link Content="Parse Clipboard" Command="{Binding Command}" CommandParameter="parseClipboard" Margin="0,2"
										UnderlineBehavior="UnderlineOnHover"/>
				<CheckBox Content="Branch Game" IsChecked="{Binding BranchGame}" IsEnabled="{Binding CanBranchGame}"/>
			</StackPanel>
			<Button DockPanel.Dock="Right" Content="Import" Command="{Binding Command}" CommandParameter="ok" IsDefault="True"/>
		</DockPanel>
	</Grid>
</dlg:DialogView>
