<dlg:DialogView x:Class="Common.Lib.UI.Controls.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Common.Lib.UI.Controls"
						 xmlns:dlg="clr-namespace:Common.Lib.UI.Dialogs;assembly=Common.Lib.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Header="{Binding Title}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<TextBlock Text="{Binding Message}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"
							 Style="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:ConfirmationDialog}},Path=MessageStyle}"/>
		<DockPanel Grid.Row="1" LastChildFill="False">
			<Button DockPanel.Dock="Right" Content="{Binding CancelButtonLabel}" Command="{Binding Command}" CommandParameter="cancel"/>
			<Button DockPanel.Dock="Right" Content="{Binding OkButtonLabel}" Command="{Binding Command}" CommandParameter="ok"/>
		</DockPanel>
	</Grid>
</dlg:DialogView>
