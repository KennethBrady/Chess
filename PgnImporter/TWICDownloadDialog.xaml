<dlg:DialogView x:Class="PgnImporter.TWICDownloadDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PgnImporter"
						 xmlns:dlg="clr-namespace:Common.Lib.UI.Dialogs;assembly=Common.Lib.UI"
						 xmlns:conv="clr-namespace:Common.Lib.UI.Converters;assembly=Common.Lib.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Height="500" Width="600">
	<dlg:DialogView.Resources>
		<conv:InverseBooleanToVisibilityConverter x:Key="ib2vc"/>
	</dlg:DialogView.Resources>
	<dlg:DialogView.Header>
		<DockPanel LastChildFill="True">
			<CheckBox DockPanel.Dock="Right" Content="Show Existing Sources" IsChecked="{Binding ShowExisting}" Foreground="White"/>
			<TextBlock Text="Download TWIC PGN Games" Foreground="White"/>
		</DockPanel>
	</dlg:DialogView.Header>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<DataGrid Grid.Row="0" ItemsSource="{Binding Entries}" IsReadOnly="True" Name="entries"/>
		<DockPanel Grid.Row="1">
			<Label Content="Destination Folder:"/>
			<Button DockPanel.Dock="Right" Content="Browse ..." Command="{Binding Command}" CommandParameter="browse"/>
			<TextBox Text="{Binding Folder}"/>
		</DockPanel>
		<DockPanel Grid.Row="2">
			<Button Content="{Binding DownloadLabel}" Command="{Binding Command}" CommandParameter="ok" IsDefault="True"/>
			<Button DockPanel.Dock="Right" Content="Cancel" IsCancel="true" Command="{Binding Command}" CommandParameter="cancel"
							Visibility="{Binding IsDownloadComplete,Converter={StaticResource ib2vc}}"/>
			<Grid>
				<ProgressBar Maximum="{Binding DownloadCount}" Minimum="0" Value="{Binding CurrentDownload,Mode=OneWay}" Foreground="Lime"
										 Visibility="{Binding IsDownloading,Converter={StaticResource b2vc}}" Height="30" VerticalAlignment="Center"/>
				<TextBlock HorizontalAlignment="Center" Text="{Binding CurrentFile}" VerticalAlignment="Center" FontSize="14"/>
			</Grid>
		</DockPanel>
	</Grid>
</dlg:DialogView>
